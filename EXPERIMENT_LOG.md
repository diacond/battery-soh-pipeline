# 실험 로그

가설 → 방법 → 결과 → 원인 분석 순서로 실험을 기록한다. 쉬운 설명은 [PROJECT_OVERVIEW.md](./PROJECT_OVERVIEW.md)를 참고한다.

## 요약

| # | 질문 | 결과 | 판정 |
|---|---|---|---|
| 1 | 방전 곡선 형태 피처가 SOH 예측을 개선하는가 | LOBO MAE 0.021 → 0.008, R² 0.924 → 0.982 | ✅ 채택 |
| 2 | 같은 피처가 RUL 예측도 개선하는가 | 평균 MAE 18.6 → 15.4, B0006은 R² 0.909 → 0.576 | ⚠️ 부분 채택 |
| 3 | 최종 모델이 처음 보는 배터리(CALCE)에도 통하는가 | R² 0.982 → 0.693, 편향 +0.053 | 📉 성능 저하, 원인 분석 |
| 4 | 편향의 원인이 정격 용량에 따른 스케일 차이인가 | 정규화 후 내부·외부 성능 모두 하락 | ❌ 기각 |
| 5 | CALCE 일부를 학습에 섞으면 나머지 CALCE를 맞히는가 | MAE 0.0022, R² 1.000 | ✅ 확인, 해석 주의 |

## 1. 요약 통계 vs 방전 곡선 형태 피처

- **가설:** 내부저항이 커질수록 방전 말기 전압이 빨리 무너진다(knee point가 앞당겨진다). 곡선 형태를 피처로 넣으면 SOH 예측이 좋아질 것이다.
- **방법:** `voltage_std`, `voltage_slope`, `time_to_knee_voltage_s`(3.0V 도달 시간), `current_std`를 추가해 피처 12개로 LOBO 평가
- **결과:**

| 홀드아웃 | 기존 MAE / R² | 추가 후 |
|---|---|---|
| B0005 | 0.023 / 0.916 | 0.003 / 0.998 |
| B0006 | 0.023 / 0.934 | 0.016 / 0.961 |
| B0007 | 0.019 / 0.925 | 0.010 / 0.973 |
| B0018 | 0.017 / 0.921 | 0.003 / 0.997 |
| **평균** | **0.021 / 0.924** | **0.008 / 0.982** |

평균 MAE가 약 2.5배 줄었다. **채택.**

## 2. 같은 피처로 RUL 예측

| 홀드아웃 | 기존 MAE / R² | 추가 후 |
|---|---|---|
| B0005 | 12.7 / 0.871 | 7.1 / 0.934 |
| B0006 | 7.2 / 0.909 | 13.7 / 0.576 |
| B0007 | 39.9 / 0.244 | 34.7 / 0.374 |
| B0018 | 14.6 / 0.727 | 6.1 / 0.941 |
| **평균** | **18.6 / 0.688** | **15.4 / 0.706** |

- 평균은 개선됐지만 B0006은 악화됐다. 새 피처가 나머지 세 배터리의 공통 패턴은 잘 잡았지만 B0006의 열화 특성과는 맞지 않은 것으로 보인다.
- B0007은 여전히 가장 어렵다(R² 0.374).
- API의 RUL 응답에 "참고용 추정치" 안내를 추가했다.

## 3. 처음 보는 배터리(CALCE) 검증

- **문제:** LOBO의 홀드아웃 배터리도 같은 실험실·장비·프로토콜로 시험한 것이다. 또 최종 모델은 4개를 모두 학습에 써서, 모델 자체를 시험할 데이터가 없었다.
- **방법:** CALCE CS2_35~38(LiCoO2, 각형, 1.1Ah)에 최종 모델을 재학습 없이 적용했다. NASA는 18650 원통형, 2.0Ah다. CALCE에는 온도 측정치가 없어 온도 피처 3개를 25°C 가정값으로 채웠다.
- **결과:**

| CALCE 셀 | MAE | R² | 실제 평균 SOH | 예측 평균 SOH |
|---|---|---|---|---|
| CS2_35 | 0.046 | 0.749 | 0.768 | 0.813 |
| CS2_36 | 0.069 | 0.654 | 0.739 | 0.806 |
| CS2_37 | 0.056 | 0.669 | 0.749 | 0.804 |
| CS2_38 | 0.050 | 0.722 | 0.759 | 0.807 |
| **전체** | **0.055** | **0.693** | | |

내부 LOBO(MAE 0.008, R² 0.982) 대비 MAE는 약 7배 커졌다.

- **원인 분석 (`feature_importances_`):**
  - `time_to_knee_voltage_s` 하나의 중요도가 **98.98%**다. 온도 피처 3개는 0이라 가정값의 영향은 없었다.
  - 이 피처는 NASA(0.999)와 CALCE(0.9998) 모두에서 SOH와 거의 완벽히 상관된다.
  - 하지만 값의 범위가 다르다: NASA 1958~3608초, CALCE 180~3716초
  - 모델이 NASA 기준의 "시간 → SOH" 대응을 학습해, CALCE에서는 4개 셀 모두 SOH를 약 0.05 과대평가했다.
  - 그래도 R² 0.65~0.75로, "전압이 빨리 무너질수록 열화가 진행됐다"는 관계 자체는 어느 정도 전이된다.

## 4. 정격 용량으로 정규화하면 편향이 줄어드는가

- **가설:** knee 도달 시간을 "정격 용량 ÷ 방전 전류"로 계산한 이론적 완전 방전 시간 대비 비율(`knee_time_ratio`)로 바꾸면 배터리 스펙이 달라도 같은 기준이 된다.
- **결과:**

| 피처 | NASA LOBO (MAE / R²) | CALCE (MAE / R²) | 평균 편향 |
|---|---|---|---|
| `time_to_knee_voltage_s` | 0.008 / 0.982 | 0.055 / 0.693 | +0.053 |
| `knee_time_ratio` | 0.016 / 0.946 | 0.072 / 0.662 | +0.067 |

**기각.** 내부·외부 모두 나빠졌다.

- **원인:** 이론적 완전 방전 시간을 계산해 보니 NASA 평균 3939초(표준편차 230초), CALCE 3601초(표준편차 1초)였다.
  - 두 데이터셋 모두 이미 약 1C로 시험돼, "스케일이 크게 다르다"는 전제가 틀렸다.
  - NASA는 사이클마다 방전 전류가 흔들려, 이 값으로 나누면서 신호에 잡음이 더해졌다(상관계수 0.999 → 0.990).
  - 편향의 실제 원인은 화학 조성·셀 형태 같은 더 근본적인 차이일 가능성이 높다.

## 5. CALCE를 일부 섞어 학습하면

- **질문:** 여러 종류의 배터리로 학습하면 같은 종류의 새 배터리를 더 잘 맞히는가? 3단계(NASA만으로 학습한 모델의 외부 일반화)와는 다른 질문이며, 3단계 결과를 대체하지 않는다.
- **방법 (`src/model/experiment_mixed_training.py`):** NASA 4개는 항상 학습에 넣고, CALCE 4개 중 3개를 추가해 학습한 뒤 남은 1개로 평가했다. 셀마다 반복했다(leave-one-cell-out).
- **결과:**

| 홀드아웃 셀 | MAE | R² | 편향 |
|---|---|---|---|
| CS2_35 | 0.0020 | 1.000 | +0.0010 |
| CS2_36 | 0.0026 | 1.000 | +0.0007 |
| CS2_37 | 0.0020 | 1.000 | +0.0003 |
| CS2_38 | 0.0023 | 1.000 | +0.0009 |
| **평균** | **0.0022** | **1.000** | **+0.0007** |

- **결과가 지나치게 좋아 원인을 확인했다:**
  - 같은 사이클 수에서 셀 간 SOH 표준편차는 CALCE 0.045, NASA 0.036으로, 셀 간 차이 자체는 있다.
  - 하지만 knee 시간과 SOH의 관계가 CALCE 4개 셀에서 같은 형태로 나타난다.
  - 4개 셀은 같은 배치·실험실·프로토콜로 시험한 셀이라, 3개만 학습해도 나머지 1개의 관계를 거의 그대로 맞힌다.
- **해석:** "다른 화학 조성의 배터리에도 통한다"가 아니라, "같은 종류의 배터리를 몇 개만 학습에 넣으면 그 종류의 나머지는 거의 정확히 예측한다"는 뜻이다. 처음 보는 종류를 보정 없이 예측하는 문제(R² 0.693)는 여전히 남아 있다.

## 정리

1. 방전 곡선 형태(특히 knee 도달 시간)는 SOH 예측에 강한 신호다. 다만 한 피처에 98.98% 의존해, 다른 배터리에서는 약점이 된다.
2. LOBO로 검증한 것은 방법론이다. 배포할 최종 모델 자체의 신뢰도는 학습에 쓰지 않은 외부 데이터로 따로 확인해야 한다.
3. 그럴듯한 가설(스케일 차이)도 검증해 보면 틀릴 수 있다.
4. 처음 보는 종류의 배터리 일반화(R² 0.693)와, 같은 종류 일부를 학습에 넣은 경우(R² 1.000)는 난이도가 전혀 다른 문제다.
5. 지나치게 좋은 결과도 원인을 확인해야 잘못된 결론을 피할 수 있다.

**실무 적용:** 새 배터리 모델을 도입할 때는 보정 없는 정확도(R² 0.693 수준)를 기준으로 안전 임계값에 여유를 둔다. 해당 모델의 샘플 셀을 몇 개 확보해 재학습하면 높은 정확도를 기대할 수 있다.

## 남은 과제

- 초기 몇 사이클로 개체차를 보정하는 방식(few-shot, 온라인 캘리브레이션)으로 처음 보는 배터리의 일반화 개선
- 온도 가정값(25°C)은 이번 모델에서 영향이 없었지만, 피처 구성이 바뀌면 결과를 왜곡할 수 있다.
- RUL에서 B0006·B0007이 어려운 원인을 곡선 형태 분석으로 확인
- 배터리·셀이 각 4개뿐이라 평균 수치가 우연일 가능성이 있다.

## 재현

```bash
pip install -r requirements.txt
bash scripts/download_data.sh && python src/pipeline/parse_mat.py && python src/model/train.py
bash scripts/download_calce.sh && python -m src.pipeline.parse_calce
python -m src.model.eval_external               # 3단계
python -m src.model.experiment_mixed_training   # 5단계
```

CI의 `external-holdout` job이 3·5단계를 매번 재현한다.
