# 실험 로그 — 가설, 결과, 그리고 왜 그렇게 나왔는가

이 문서는 `battery-soh-pipeline` 프로젝트에서 실제로 세운 가설과, 그
가설을 검증한 결과, 결과가 예상과 다를 때 원인을 어떻게 추적했는지를
시간 순서대로 정리한 기술 로그입니다. `README.md`에 결과가 흩어져
있어서, 면접 전에 "어떤 순서로 무엇을 시도했고 왜 그런 판단을
내렸는지"를 한 번에 복기할 수 있도록 별도로 모았습니다. 배터리 지식이
없어도 읽을 수 있는 요약은 `PROJECT_OVERVIEW.md`를 참고하세요 — 이
문서는 그보다 기술적으로 상세합니다.

## 한눈에 보는 타임라인

| # | 질문/가설 | 방법 | 결과 | 판정 |
|---|---|---|---|---|
| 1 | 사이클 요약 통계만으로 충분한가, 방전 곡선의 "형태"를 피처로 추가하면 나아지는가 | 전압 표준편차·기울기·knee 도달 시간 등 곡선 형태 피처 추가 | SOH LOBO 평균 MAE 0.021→0.008 (R² 0.924→0.982) | ✅ 채택 |
| 2 | 새 피처가 RUL 예측에도 똑같이 도움되는가 | 같은 피처로 RUL 모델 재학습 | 평균은 개선(MAE 18.6→15.4)됐지만 B0006은 악화(R² 0.909→0.576) | ⚠️ 부분 채택, 한계 기록 |
| 3 | LOBO로 검증한 모델이 진짜 미지의 배터리에도 통하는가 | NASA 4개 학습 모델을 CALCE(전혀 다른 배터리)에 재학습 없이 평가 | R² 0.982→0.693, MAE 0.008→0.055, 편향 +0.053 | 📉 성능 저하 확인, 원인 규명 |
| 4 | 편향의 원인은 배터리 스펙(정격 용량·전류) 차이에 따른 피처 스케일 불일치인가 | `time_to_knee_voltage_s`를 정격 용량 기준 비율(`knee_time_ratio`)로 정규화 | 내부 R² 0.982→0.946, 외부 R² 0.693→0.662로 둘 다 악화 | ❌ 가설 기각 |
| 5 | 그렇다면 학습 단계에서부터 CALCE를 일부 섞으면 도움이 되는가 | NASA 4개 + CALCE 3개로 학습 → CALCE 나머지 1개로 평가(leave-one-cell-out, 4회 반복) | MAE 0.0022, R² 1.000, 편향 +0.0007 | ✅ 확인, 단 해석에 주의 필요 |

아래는 각 항목을 상세히 풀어 쓴 것입니다.

## 1단계 — 베이스라인: 요약 통계 vs 곡선 형태 피처

**배경.** 처음에는 사이클당 전압·전류·온도의 평균/최솟값 정도(요약
통계)만 피처로 썼습니다.

**가설.** 배터리 내부저항이 커질수록 방전 말기에 전압이 더 빨리
무너지는(knee point가 앞당겨지는) 경향이 있으므로, 이 "곡선의 형태"
정보를 피처로 추가하면 SOH 예측이 더 정확해질 것이다.

**방법.** `voltage_std`(전압 표준편차), `voltage_slope`(전압-시간
선형회귀 기울기), `time_to_knee_voltage_s`(전압이 3.0V 밑으로
떨어지기까지 걸린 시간), `current_std` 4개 피처를 추가(총 12개 피처).
검증은 Leave-One-Battery-Out(LOBO) — 배터리 4개 중 1개를 완전히
숨기고 나머지 3개로 학습한 뒤 숨긴 배터리로 평가하는 것을 4번
반복해서 평균.

**결과(SOH 예측):**

| 홀드아웃 배터리 | 기존 MAE / R² | 곡선 피처 추가 후 MAE / R² |
|---|---|---|
| B0005 | 0.023 / 0.916 | 0.003 / 0.998 |
| B0006 | 0.023 / 0.934 | 0.016 / 0.961 |
| B0007 | 0.019 / 0.925 | 0.010 / 0.973 |
| B0018 | 0.017 / 0.921 | 0.003 / 0.997 |
| **평균** | **0.021 / 0.924** | **0.008 / 0.982** |

가설대로 뚜렷하게 개선(LOBO 평균 MAE 약 2.5배 감소). **채택.**

## 2단계 — 같은 피처, RUL(잔여 수명) 예측에서는?

**가설.** SOH에 도움이 됐던 피처라면 RUL(같은 사이클 데이터에서
End-of-Life까지 남은 사이클 수를 예측하는 것) 예측에도 비슷하게
도움이 될 것이다.

**결과:**

| 홀드아웃 배터리 | 기존 MAE / R² | 곡선 피처 추가 후 MAE / R² |
|---|---|---|
| B0005 | 12.7 / 0.871 | 7.1 / 0.934 |
| B0006 | 7.2 / 0.909 | 13.7 / 0.576 |
| B0007 | 39.9 / 0.244 | 34.7 / 0.374 |
| B0018 | 14.6 / 0.727 | 6.1 / 0.941 |
| **평균** | **18.6 / 0.688** | **15.4 / 0.706** |

**판정: 부분적으로만 맞았다.** 평균은 개선됐지만 B0006은 오히려
나빠졌다(R² 0.909→0.576). 새 피처가 B0005·B0007·B0018 세 배터리의
공통 열화 패턴은 더 잘 잡아냈지만, 그 패턴이 B0006의 개별 특성과는
안 맞아서 이 배터리를 홀드아웃했을 때 일반화가 나빠진 것으로 해석했다
— "피처를 늘린다고 모든 배터리가 다 좋아지는 게 아니다"라는 걸 그대로
기록했다. B0007은 피처를 추가해도 여전히 가장 어려운 배터리라(R²
0.374), API 응답에 "RUL은 참고용, SOH만큼 신뢰하지 말 것"이라는
안내를 추가했다.

## 3단계 — LOBO의 숨은 한계: "새 배터리"가 진짜 새 배터리인가?

**문제 인식.** 위 LOBO 검증과 실제 서비스에 배포한 최종 모델
(`soh_model.joblib`, NASA 4개 배터리를 전부 학습에 사용)에는 구조적
한계가 있었다. LOBO의 4라운드는 매번 배터리 1개를 "새 배터리"로
취급했지만, 그 배터리도 결국 같은 실험실·같은 장비·같은 프로토콜로
만든 배터리였다. 그리고 최종 모델은 4개를 전부 학습에 써버려서, 이
모델 자체를 시험해볼 홀드아웃 데이터가 아예 없었다. 즉 "검증된
방법론으로 만든 모델"이었지 "검증된 모델 그 자체"는 아니었다.

**가설.** 이 최종 모델을 학습에 단 한 번도 쓰이지 않은, 완전히 다른
제조사·스펙의 배터리에 그대로 적용하면 성능이 크게 떨어질 것이다
(정확히 얼마나 떨어지는지, 왜 떨어지는지는 실측 전까지 미지수).

**방법.** CALCE(메릴랜드대) CS2 시리즈 셀 4개(CS2_35~38)를 확보했다.
NASA(18650 원통형, 정격 2.0Ah)와 달리 CALCE CS2는 각형(prismatic,
5.4x33.6x50.6mm) 셀에 LiCoO2 양극재, 정격 1.1Ah로 셀 형태부터
다르다. `soh_model.joblib`을 재학습 없이 그대로 이 데이터에
`predict()`했다(`src/pipeline/parse_calce.py`, `src/model/
eval_external.py`).

호환성 문제 하나는 미리 타협해야 했다: CALCE 원본(Arbin 테스터
로그)에는 온도 측정치가 없어서, `temperature_mean`/`temperature_max`/
`ambient_temperature_c` 세 피처는 CALCE의 상온 시험 프로토콜에 근거해
25도 고정값(가정값, 실측 아님)으로 채워 넣었다.

**결과:**

| CALCE 셀 | MAE | R² | 실제 평균 SOH | 예측 평균 SOH |
|---|---|---|---|---|
| CS2_35 | 0.046 | 0.749 | 0.768 | 0.813 |
| CS2_36 | 0.069 | 0.654 | 0.739 | 0.806 |
| CS2_37 | 0.056 | 0.669 | 0.749 | 0.804 |
| CS2_38 | 0.050 | 0.722 | 0.759 | 0.807 |
| **전체** | **0.055** | **0.693** | | |

NASA 내부 LOBO 평균(MAE 0.008, R² 0.982)과 비교하면 MAE는 약 7배
나빠지고 R²는 0.98→0.69로 떨어졌다.

**원인 분석 (`model.feature_importances_`).** 이 모델은 12개 피처
중 `time_to_knee_voltage_s` 단 하나에 **98.98%**를 의존하고 있었다
(나머지 11개를 합쳐도 1.02%). 걱정했던 온도 가정값은 실제로는 거의
영향이 없었다 — 온도 관련 3개 피처의 중요도는 전부 0.0000이었다.

이 knee 피처는 NASA 안에서도(SOH와 상관계수 0.999), CALCE 안에서도
(상관계수 0.9998) SOH와 거의 완벽하게 상관돼 있다. 문제는 이
상관관계의 **숫자 스케일**이 데이터셋마다 다르다는 것이다:

- NASA: 2.0Ah 배터리를 약 2A로 방전 → knee 도달 시간 1958~3608초
- CALCE: 1.1Ah 배터리를 약 1.1A로 방전 → knee 도달 시간 180~3716초

모델은 NASA의 스케일에 맞춰 "이 정도 시간 = 이 정도 SOH"라는 대응
관계를 학습했기 때문에, 스펙이 다른 배터리를 만나면 이 대응이
어긋나면서 SOH를 체계적으로 과대평가하는 편향이 생겼다(4개 셀 모두
예측 평균이 실제보다 약 0.05 높다). 그래도 R² 0.65~0.75는 우연(0)보다
뚜렷하게 나아서, "방전 말기 전압 붕괴 시점이 당겨질수록 열화가
진행된 것"이라는 물리적 관계 자체는 어느 정도 전이된다는 것도
확인했다.

## 4단계 — 가설: 정격 용량으로 정규화하면 편향이 줄어들까

**가설.** `time_to_knee_voltage_s`를 초 단위 절대값 대신, "정격 용량
÷ 방전 전류"로 계산한 이론적 완전방전 시간 대비 비율
(`knee_time_ratio`)로 바꾸면, 배터리 스펙이 달라도 같은 잣대가 되어
편향이 줄어들 것이다.

**방법.** `src/model/experiment_normalized_feature.py`. 실제 서비스
모델·API 스키마는 건드리지 않고, `train.py`/`eval_external.py`에
`feature_columns` 선택 인자를 추가(하위 호환 유지)해서 대안 피처
조합으로만 별도 실험했다.

**결과:**

| | NASA 내부 LOBO (MAE/R²) | CALCE 외부 홀드아웃 (MAE/R²) | 평균 편향 |
|---|---|---|---|
| 기존(`time_to_knee_voltage_s`) | 0.008 / 0.982 | 0.055 / 0.693 | +0.053 |
| 정규화(`knee_time_ratio`) | 0.016 / 0.946 | 0.072 / 0.662 | +0.067 |

**판정: 가설 기각.** 내부·외부 검증 둘 다 오히려 나빠졌다.

**왜 틀렸는가.** "이론적 완전방전 시간"(정격 용량÷전류)을 실제로
계산해보니:

- NASA: 평균 3939초, 표준편차 230초
- CALCE: 평균 3601초, 표준편차 1초

두 데이터셋 모두 이미 거의 1C(정격 용량만큼의 전류) 근처로 시험되고
있었다 — 즉 "스케일이 몇 배씩 다르다"는 전제 자체가 틀렸다. 반면
NASA는 사이클마다 방전 전류가 실제로 조금씩 흔들리는데(분모의 표준
편차가 230초로 큼), 이 흔들리는 값으로 나누면서 원래 거의
완벽했던 신호(NASA 내부 상관계수 0.999)에 잡음만 더 얹은 꼴이
됐다(정규화 후 0.990으로 하락). 진짜 원인은 스펙 차이에 따른
스케일 문제가 아니라, 두 배터리의 화학 조성·셀 형태(NASA는 18650
원통형, CALCE는 각형)에서 오는 더 근본적인 차이일 가능성이 높다 —
이건 단순 비율 계산으로는 고쳐지지 않는다.

## 5단계 — 학습 단계에서 CALCE를 섞으면 어떨까 (다른 질문)

**가설을 세우기 전에 먼저 짚은 것.** 이건 3단계·4단계와는 다른
질문이다. `eval_external.py`는 "NASA만으로 배운 모델이 완전히 낯선
배터리에 통하는가"를 물었다. 이번 질문은 "다양한 배터리로 배우면 또
다른 새 배터리에 더 잘 통하는가"다. 후자가 어떤 결과를 내든 전자의
결과(3단계)를 대체하거나 무효화하지 않는다 — 서로 다른 질문에 대한
서로 다른 답이다.

**방법론적 주의사항.** CALCE 4개를 전부 학습에 넣고 같은 CALCE로
평가하면 자기 자신으로 시험 보는 셈이라 데이터 누수다. 그래서 NASA
배터리에 썼던 LOBO 원칙을 CALCE 쪽에도 그대로 확장했다: NASA 4개는
항상 학습에 포함하고, CALCE 4개 중 3개를 추가로 섞어 학습한 뒤,
학습에 전혀 쓰이지 않은 나머지 1개로만 평가한다. 이걸 CALCE 4개
각각 돌아가며 4번 반복했다(`src/model/experiment_mixed_training.py`).

**결과:**

| held-out 셀 | MAE | R² | 편향 |
|---|---|---|---|
| CS2_35 | 0.0020 | 1.000 | +0.0010 |
| CS2_36 | 0.0026 | 1.000 | +0.0007 |
| CS2_37 | 0.0020 | 1.000 | +0.0003 |
| CS2_38 | 0.0023 | 1.000 | +0.0009 |
| **평균** | **0.0022** | **1.000** | **+0.0007** |

NASA만으로 학습한 모델(MAE 0.055 / R² 0.693 / 편향 +0.053)과
비교하면 거의 완벽하게 해결된 것처럼 보인다.

**그런데 R²=1.000을 그대로 믿지 않고 의심해봤다.** 이 프로젝트
전체에서 일관되게 취한 태도("결과가 너무 좋으면 먼저 의심한다")를
여기서도 적용했다. 왜 이렇게 잘 나왔는지 확인한 결과:

- 같은 사이클 수에서 셀 간 SOH 표준편차: CALCE 0.045, NASA 0.036
  (CALCE 쪽이 오히려 조금 더 크다 — 셀 간 다양성 자체는 있다)
- 그럼에도 `time_to_knee_voltage_s`가 개별 셀 안에서 SOH와 거의
  완벽하게(상관계수 0.9998) 대응하는 관계 자체는 CALCE 4개 셀 모두
  동일한 형태로 나타난다

즉 CALCE 4개 셀은 같은 제조 배치·같은 실험실·같은 프로토콜로 거의
동시에 시험된 "형제 배터리"들이라, 셀 3개만 봐도 그 관계의 "모양"을
거의 완전히 파악할 수 있고, 4번째 셀도 같은 모양을 따르므로 거의
완벽하게 맞힌 것이다.

**정확한 해석.** 이 실험이 증명한 것은 "전혀 다른 화학 조성의
배터리에도 통한다"가 아니라, **"같은 종류(같은 제조 배치)의 배터리를
몇 개만 섞어 학습해도 그 종류의 나머지 배터리는 거의 완벽하게 예측할
수 있다"**는 것이다. 3단계의 "완전 미지의 배터리 종류를 처음부터
무보정으로 예측하는 것"이 여전히 어려운 문제(R² 0.693)로 남아있다는
사실은 이 실험으로 바뀌지 않는다 — 두 실험은 서로 다른 질문에
답한다.

## 종합: 결국 무엇을 알아냈는가

1. 사이클 요약 통계보다 방전 곡선의 형태(특히 knee point 도달 시간)가
   SOH 예측에 훨씬 강력한 신호다 — 다만 이 신호가 지나치게 지배적
   (98.98%)이라 다른 배터리 스펙에서는 오히려 취약점이 됐다.
2. "검증된 방법론"과 "검증된 모델"은 다르다. LOBO로 방법론을 검증해도,
   실제 배포되는 최종 모델 자체는 진짜 홀드아웃 없이는 신뢰도를 알 수
   없다 — 이걸 실측으로 확인하려면 정말 학습에 안 쓰인 외부 데이터가
   필요하다.
3. "성능이 떨어지는 이유"에 대한 그럴듯한 가설(스케일 불일치)이 항상
   맞는 건 아니다 — 실제로 검증해보니 전제 자체가 틀렸고, 진짜 원인은
   더 근본적인 배터리 특성 차이였다.
4. "완전히 낯선 배터리에 대한 일반화"와 "이미 일부를 학습에 포함한
   같은 종류 배터리에 대한 일반화"는 완전히 다른 난이도의 문제다.
   후자는 사실상 해결되지만(R²=1.000), 전자는 여전히 어렵다(R²=0.693).
5. 너무 좋은 결과(R²=1.000)는 실패만큼이나 의심해봐야 한다 — 원인을
   추적하지 않았다면 "혼합 학습으로 문제를 완전히 해결했다"는 잘못된
   결론을 내렸을 것이다.

## 실무적 시사점

배터리 진단 서비스를 실제로 운영한다면: 완전히 새로운 배터리 모델을
처음 도입하는 시점에 무보정으로 예측하는 정확도(R² 0.693 수준)를
기대치로 잡아야 하고, 안전 관련 임계값 판단에는 그만큼 보수적인
여유를 둬야 한다. 반면 그 배터리 모델의 샘플 셀을 몇 개(3개 정도도
충분)만 확보해서 재학습(캘리브레이션)할 수 있다면, 그 모델 전체에
대해 매우 높은 정확도(R²≈1.0)를 기대할 수 있다 — 즉 "신규 배터리
도입 시 최소 몇 개의 샘플 확보"가 실무적으로 중요한 절차가 된다.

## 남은 과제

- CALCE 4개를 전혀 쓰지 못하는 "진짜 첫 도입 시점"의 zero-shot
  일반화(R² 0.693)를 개선하는 방법 — 배터리별 초기 몇 사이클만으로
  개체차를 빠르게 보정하는 few-shot/온라인 캘리브레이션 방식 검토
- CALCE에 온도 실측치가 없어 가정값(25도 고정)을 넣었는데, 이번
  모델에서는 온도 피처 중요도가 0이라 문제가 안 됐다. 하지만 피처
  구성이 달라지면 이 가정이 결과를 왜곡할 수 있다는 점은 계속 유의
- RUL 예측에서 B0006이 새 피처로 오히려 나빠진 원인, B0007이 여전히
  어려운 원인을 곡선 형태 클러스터링 등으로 더 파고들기
- `docker-compose up`이 실제 Docker 환경에서 이상 없이 뜨는지는
  MSA 절에서 별도로 검증됨(README 참고)
- 배터리 4개(NASA)·셀 4개(CALCE)로는 폴드 수 자체가 적어서, 모든
  평균 수치가 "운 좋게 잘 나온 값"일 위험은 항상 남아있다

## 재현 방법

```bash
pip install -r requirements.txt
bash scripts/download_data.sh && python src/pipeline/parse_mat.py && python src/model/train.py
bash scripts/download_calce.sh && python -m src.pipeline.parse_calce
python -m src.model.eval_external              # 3단계
python -m src.model.experiment_normalized_feature  # 4단계
python -m src.model.experiment_mixed_training      # 5단계
```

`.github/workflows/ci.yml`의 `external-holdout` job이 이 순서
그대로를 push/PR마다 처음부터 재현해서 검증한다.
