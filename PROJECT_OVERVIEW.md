# 배터리 SOH 진단 프로젝트 — 쉽게 풀어쓴 설명서

이 문서는 `README.md`(기술 문서)와 별개로, 배터리에 대한 배경지식이 없어도
"이 프로젝트가 뭘 하는 건지, 왜 이렇게 만들었는지"를 이해할 수 있도록
풀어쓴 설명서입니다. 면접이나 자기소개 자리에서 이 프로젝트를 설명할 때
참고용으로 쓰시면 됩니다.

---

## 1. 한 줄 요약

**"배터리가 충전과 방전을 반복하면서 얼마나 닳았는지(성능이 떨어졌는지)를
숫자로 예측하고, 그 결과를 실시간으로 알려주는 작은 서비스를 만들었다."**

## 2. 왜 이 문제가 중요한가

휴대폰 배터리를 오래 쓰면 하루 종일 가던 게 반나절도 못 가게 되죠.
전기차나 ESS(에너지 저장장치)도 똑같습니다. 배터리는 충전·방전을
반복할수록 "원래 담을 수 있던 전기 양"이 점점 줄어듭니다. 이걸
**열화(degradation)**라고 부릅니다.

문제는 이 열화가 배터리마다, 쓰는 방식마다(온도, 얼마나 세게 쓰는지 등)
속도가 다 다르다는 겁니다. 그래서 "지금 이 배터리가 원래 성능의 몇
%나 남았는지", "언제쯤 교체해야 하는지"를 미리 알 수 있으면, 갑자기
배터리가 퍼져서 사고가 나거나, 아직 멀쩡한데 미리 교체해서 돈을 낭비하는
일을 막을 수 있습니다. 전기차 회사나 배터리 회사(LG에너지솔루션 같은
곳)가 실제로 이걸 서비스로 만들어서 판매합니다. 이 프로젝트는 그 서비스를
아주 작은 규모로 직접 만들어본 것입니다.

## 3. 핵심 용어 세 가지

프로젝트를 이해하려면 이 세 단어만 알면 됩니다.

- **SOH (State of Health, 건강 상태)** — 배터리가 "지금 원래 성능의
  몇 %인지"를 나타내는 숫자입니다. 100%면 신품, 70%면 원래 담을 수
  있던 전기의 70%만 담을 수 있다는 뜻입니다. 보통 70% 밑으로 떨어지면
  "수명이 다했다(교체 시점)"고 봅니다.
- **RUL (Remaining Useful Life, 잔여 수명)** — "앞으로 몇 번 더
  충방전할 수 있는지(몇 사이클 남았는지)"를 나타내는 숫자입니다. SOH가
  "지금 상태"라면, RUL은 "앞으로의 예측"입니다.
- **사이클(cycle)** — 배터리를 한 번 충전했다가 완전히 방전하는 것을
  1사이클이라고 합니다. 이 프로젝트에서 쓴 배터리들은 수백 사이클 동안
  반복 실험한 기록이 남아있습니다.

## 4. 어떤 데이터를 썼는가

NASA(미국 항공우주국)가 실제로 배터리 4개를 각각 수백 번씩 충방전시키면서
전압, 전류, 온도, 그리고 그 순간의 실제 용량을 다 기록해서 공개한
데이터셋을 사용했습니다. 방산·항공우주 쪽에서도 이 데이터를 예지정비
연구에 자주 씁니다.

이 데이터에서 "방전이 끝날 때까지 기록된 전압·전류·온도 그래프"를
사이클마다 하나씩 뽑아서, 그 그래프의 특징(평균, 최솟값, 얼마나
가파르게 떨어지는지 등)을 숫자로 요약했습니다. 이 요약된 숫자들이
바로 모델이 보고 배우는 재료(피처)입니다.

## 5. 무엇을 만들었는가 — 전체 그림

```
① 원본 데이터(전압/전류/온도 그래프)
        ↓  숫자로 요약
② 사이클별 요약표 (엑셀 표처럼 생긴 데이터)
        ↓  패턴 학습
③ 예측 모델 (SOH 예측 모델 1개 + RUL 예측 모델 1개)
        ↓  실시간으로 답해주는 창구
④ API (누가 물어보면 "지금 몇 % 남았다"고 즉시 답해주는 서버)
        ↓  숫자를 말로 풀어서 설명
⑤ 설명 레이어 (사람이 읽기 쉬운 문장으로 바꿔주는 부분, 필요하면 AI 사용)
```

이 다섯 단계를 각각 코드로 만들고, 실제로 서버를 띄워서 요청을 보내면
답이 오는 것까지 확인했습니다.

## 6. 왜 이렇게 검증했는가 — 가장 중요한 설계 판단

**"한 번도 안 본 배터리"를 시험 문제로 냈습니다.** 처음엔 배터리
4개의 사이클을 전부 섞어서 무작위로 "공부할 것"과 "시험 볼 것"을
나눴는데, 그러면 점수가 비현실적으로 잘 나옵니다. 같은 배터리의 바로
옆 사이클끼리는 상태가 거의 똑같아서, 모델이 "공부"가 아니라 "답을
미리 봤다"에 가깝기 때문입니다.

그래서 방식을 바꿨습니다. 배터리 4개 중 3개로만 공부시키고, 나머지
1개는 모델이 한 번도 본 적 없는 상태로 시험을 봤습니다. 이걸 4번
반복해서(매번 다른 배터리를 시험 문제로) 평균을 냈습니다. 이렇게
하니 점수는 좀 낮아졌지만, "새로 들어온 배터리를 실제로 얼마나 잘
맞히는지"를 훨씬 정직하게 보여주는 숫자가 나왔습니다.

**결과: SOH 예측은 평균적으로 실제 값과 오차가 약 0.8%p밖에 안
났습니다(100점 만점에 99.2점 맞힌 셈).** 곡선의 모양(전압이 얼마나
가파르게 떨어지는지 등)을 피처로 추가하고 나서 이 정도까지
좋아졌습니다.

**RUL(잔여 수명) 예측은 상대적으로 더 어려웠습니다.** 배터리 4개 중
하나(B0007)는 다른 셋과 열화 패턴 자체가 달라서, 그 배터리를 시험
문제로 냈을 때는 예측이 잘 안 맞았습니다. 이걸 숨기지 않고 결과에
그대로 남겨뒀고, API도 "이 예측값은 참고만 하고 전적으로 믿지는
말라"는 안내 문구를 같이 보내주게 만들었습니다. 모델이 모든 상황에서
완벽하지 않다는 걸 정직하게 인정하는 것도 실무에서는 중요한 태도라고
생각했습니다.

## 7. AI(생성형 AI)는 어디에, 왜 썼는가

예측값(숫자)을 사람이 읽기 편한 문장으로 바꿔주는 부분에만 AI를
썼습니다. 예를 들어 "SOH 0.87"이라는 숫자를 "지금 상태는 정상 범위지만
온도가 좀 높게 관측됐다"처럼 자연스러운 문장으로 바꿔주는 역할입니다.

중요한 건, **"위험한지 아닌지 판단하는 부분에는 AI를 쓰지 않았다"**는
점입니다. 그 판단은 미리 정해둔 규칙(숫자 비교)이 하고, AI는 그 결과를
말로 풀어 설명하는 것만 담당합니다. 그리고 AI 호출이 실패하거나
설정이 안 돼 있어도 서비스가 멈추지 않고, 미리 준비해둔 규칙 기반
설명으로 자동으로 대체되도록 만들었습니다. AI를 "있으면 좋은
보조 기능"으로만 쓰고, 핵심 판단은 AI에 맡기지 않은 게 의도적인
설계입니다.

## 8. 왜 서비스를 여러 개로 쪼갰는가 (MSA)

처음엔 예측하고, 설명하고, API로 답해주는 걸 프로그램 하나로 다
만들었습니다. 그런데 실제 회사에서는 보통 "예측 모델을 새로 학습해서
바꾸는 일"과 "설명 문구나 AI 프롬프트를 다듬는 일"이 서로 다른
사람이, 다른 주기로 하는 경우가 많습니다. 하나로 묶여 있으면 문구
하나 고치는 것도 예측 서비스 전체를 다시 배포해야 하는 번거로움이
생깁니다.

그래서 "예측만 하는 서비스", "설명만 하는 서비스", 그리고 이 둘을
순서대로 불러서 답을 조립해주는 "게이트웨이"까지 세 개의 독립된
프로그램으로 나눠봤습니다. 각각 별도의 컨테이너(Docker)로 띄워서
실제로 서로 네트워크로 통신하며 정상 동작하는 것까지 확인했습니다.
설명 서비스가 갑자기 멈추더라도 예측 결과 자체는 계속 내려주도록
만들어서, 하나가 고장 나도 전체가 같이 멈추지 않게 하는 것도
신경 썼습니다.

## 9. Docker와 CI는 뭘 해주는가

**Docker**는 "이 프로그램이 어떤 컴퓨터에서 실행되든 항상 똑같이
동작하도록 포장하는 기술"입니다. 제 컴퓨터에서 되는 게 남의 컴퓨터에서
안 되는 문제를 막아줍니다. 이 프로젝트를 컨테이너 3개로 실제로
띄워서, 서로 통신해서 정상적으로 답을 주는 것까지 직접 확인했습니다.

**CI(지속적 통합, GitHub Actions)**는 "코드가 바뀔 때마다 자동으로
처음부터 다시 돌려보고 문제없는지 검사해주는 로봇"입니다. 데이터
다운로드부터 모델 학습, 테스트까지 전체 과정을 매번 자동으로
재현해서, 누군가 이 코드를 새로 받았을 때도 문서에 적힌 대로 하면
똑같이 잘 돌아간다는 걸 보장해줍니다.

## 10. 진짜 "처음 보는 배터리"로 시험해봤더니

6번에서 "배터리 4개 중 1개를 숨겨놓고 시험 보는" 방법(LOBO)을
설명했는데, 사실 이 시험엔 숨은 함정이 있었습니다. 숨겨놓은
배터리도 결국 같은 실험실, 같은 장비, 같은 방식으로 만든
배터리였다는 것입니다. 완전히 다른 회사·다른 제품의 배터리를
만났을 때도 잘 맞히는지는 실제로 확인한 적이 없었습니다.

그래서 진짜로 확인해봤습니다. 모델이 한 번도 배운 적 없는, 완전히
다른 대학(메릴랜드대)이 다른 종류의 배터리(용량도 더 작은 CS2
시리즈)로 만든 데이터를 가져와서, 우리 모델에게 그대로 "이
배터리는 지금 몇 %야?"라고 물어봤습니다.

**결과: 원래 시험(같은 조건 배터리끼리)에서는 100점 만점에 99.2점
(R² 0.982)이었는데, 완전히 낯선 배터리로 시험을 보니 69점(R²
0.693) 정도로 떨어졌습니다.** 오차도 원래는 0.8%p 정도였는데
이번엔 5.5%p로 커졌습니다.

왜 이렇게 떨어졌는지도 찾아봤습니다. 알고 보니 이 모델은 "방전
중 전압이 특정 기준(3.0V) 밑으로 떨어지기까지 걸리는 시간" 딱
한 가지 정보에 거의 전적으로(99%) 의존해서 판단하고 있었습니다.
이 자체는 나쁜 신호가 아닙니다 — 배터리가 낡을수록 이 시간이
짧아지는 건 물리적으로 맞는 얘기고, 두 데이터셋 모두에서 이 값과
실제 상태는 거의 완벽하게 비례했습니다. 문제는 "이 정도 시간이면
이 정도 상태"라는 정확한 대응 관계가 배터리 용량(원래 배터리는
2.0Ah, 새 배터리는 1.1Ah)에 따라 다르다는 걸 모델이 몰랐다는
겁니다. 그래서 새 배터리를 실제보다 살짝 더 건강하다고(평균
5%p 정도) 착각하는 경향을 보였습니다.

그래도 완전히 틀린 건 아니었습니다. 69점이면 "찍는 것보다는
확실히 낫다"는 뜻이고, "전압이 빨리 무너질수록 배터리가
낡았다"는 기본 원리 자체는 다른 배터리에도 어느 정도 통한다는
걸 보여줬습니다. 다만 배터리 종류가 바뀌면 그 원리를 적용하는
"눈금"은 다시 맞춰야 한다는 게 이번 실험에서 새로 확인한
사실입니다.

## 11. 한계와 배운 점 (정직하게)

- 배터리가 4개뿐이라 "새 배터리를 시험 본" 경우도 4번뿐입니다.
  숫자가 우연히 잘 나온 것일 수도 있어서, 실제로는 배터리가 더 많이
  필요합니다.
- 새로운 피처를 추가했을 때 전체 평균은 좋아졌지만, 배터리 하나
  (B0006)에서는 오히려 예측이 나빠졌습니다. "피처를 늘린다고 항상
  다 좋아지는 건 아니다"라는 걸 직접 경험했습니다.
- Docker로 여러 서비스를 나누는 것까지는 실제로 확인했지만, 이보다
  더 큰 규모(트래픽이 많을 때, 서버가 여러 대일 때)에서는 어떻게
  될지는 이 프로젝트 범위 밖입니다.
- 10번에서 본 것처럼, 완전히 다른 배터리로 넘어가면 정확도가
  꽤 떨어집니다. 이 프로젝트의 모델은 "같은 종류의 배터리라면
  믿을 만하다" 정도지, "어떤 배터리든 다 맞힌다"는 아닙니다.

## 12. 이 프로젝트로 보여줄 수 있는 것

- 데이터를 있는 그대로 믿지 않고, 평가 방법 자체를 의심하고
  고쳐본 경험 (무작위 분할 → 배터리 단위 분할)
- AI를 어디에 쓰고 어디에 안 쓸지 스스로 판단한 경험 (설명에만
  사용, 판단은 규칙 기반)
- 실제 서비스 구조(마이크로서비스, 컨테이너, 자동화 테스트)를
  작게라도 직접 만들어보고 손으로 검증한 경험
- 좋은 결과만 골라 보여주지 않고, 안 되는 부분(B0006, B0007)까지
  정직하게 기록하고 이유를 설명하려 한 태도
- "검증됐다"는 말을 그대로 믿지 않고, 그 검증이 정말 무엇을
  검증한 것인지 스스로 의심해서 실제로 확인까지 해본 경험
  (같은 실험실 배터리 간 검증 → 완전히 다른 배터리로 재검증)
