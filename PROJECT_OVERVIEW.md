# 프로젝트 개요 (쉽게 풀어쓴 설명)

배터리 배경지식 없이도 이 프로젝트가 무엇을 하고 왜 이렇게 만들었는지 이해할 수 있도록 정리한 문서다. 기술적인 내용은 [README.md](./README.md)와 [EXPERIMENT_LOG.md](./EXPERIMENT_LOG.md)를 참고한다.

## 한 줄 요약

배터리가 충방전을 반복하며 얼마나 닳았는지 예측하고, 그 결과를 API로 알려 주는 서비스를 만들었다.

## 왜 필요한가

배터리는 쓸수록 담을 수 있는 전기 양이 줄어든다(열화). 열화 속도는 배터리와 사용 방식마다 달라서, 남은 성능과 교체 시점을 미리 알면 갑작스러운 고장과 불필요한 조기 교체를 모두 줄일 수 있다. 전기차·ESS 업계에서 실제로 서비스하는 문제를 작은 규모로 구현했다.

## 핵심 용어

| 용어 | 뜻 |
| --- | --- |
| SOH (State of Health) | 현재 성능이 신품 대비 몇 %인지. 보통 70% 미만을 교체 시점으로 본다 |
| RUL (Remaining Useful Life) | 앞으로 더 쓸 수 있는 충방전 횟수 |
| 사이클 | 한 번 충전하고 방전하는 과정 |

## 전체 흐름

```
① 충방전 기록(전압·전류·온도 그래프)
   ↓ 숫자로 요약
② 사이클별 요약표
   ↓ 학습
③ 예측 모델 (SOH, RUL)
   ↓
④ API: 요청하면 "현재 몇 %"를 바로 응답
   ↓
⑤ 설명: 숫자를 읽기 쉬운 문장으로 변환
```

## 설계 포인트

**1. 처음 보는 배터리로 시험했다.**
사이클을 무작위로 섞어 학습·시험을 나누면, 같은 배터리의 비슷한 사이클이 양쪽에 들어가 점수가 부풀려진다. 배터리 4개 중 3개로 학습하고 남은 1개로 시험하는 방식을 4번 반복했다. 그 결과 SOH 오차는 평균 약 0.8%p였다.

**2. AI는 설명에만 썼다.**
"교체가 필요한가"는 정해진 규칙(숫자 비교)이 판단하고, 생성형 AI는 결과를 문장으로 풀어 주기만 한다. AI 호출이 실패해도 규칙 기반 문장으로 대체되어 서비스가 멈추지 않는다.

**3. 서비스를 셋으로 나눈 버전도 만들었다.**
모델 교체와 설명 문구 수정은 주기가 다르다. 예측 서비스, 설명 서비스, 둘을 묶는 게이트웨이로 나눠 한쪽을 고쳐도 다른 쪽을 다시 배포하지 않아도 되게 했다. 설명 서비스가 멈춰도 예측 결과는 그대로 반환한다. 컨테이너를 실제로 띄워 보는 검증은 아직 하지 않았다.

**4. CI로 전체 과정을 재현한다.**
코드가 바뀔 때마다 GitHub Actions가 데이터 다운로드부터 학습, 테스트까지 처음부터 다시 실행한다.

## 다른 종류의 배터리에도 통할까

위 시험에 쓴 배터리 4개는 모두 같은 실험실에서 같은 방식으로 시험한 것이다. 그래서 다른 대학(메릴랜드대)이 시험한, 용량도 다른 배터리(CALCE CS2) 데이터에 모델을 그대로 적용해 봤다.

- 정확도(R²)가 0.982에서 0.693으로 떨어졌고, 오차는 약 0.8%p에서 5.5%p로 커졌다.
- 모델은 "방전 중 전압이 3.0V 아래로 떨어지기까지 걸린 시간" 하나에 약 99% 의존하고 있었다.
- 이 시간은 배터리가 낡을수록 짧아지므로 좋은 신호지만, 시간과 성능의 대응 관계가 배터리 종류마다 달라 새 배터리를 평균 약 5%p 건강하게 보는 편향이 생겼다.
- 기본 원리는 다른 배터리에도 어느 정도 통하지만, 배터리 종류가 바뀌면 보정이 필요하다는 것을 확인했다.

## 한계

- 배터리가 4개뿐이라 결과가 우연일 가능성이 있다.
- 새 피처를 넣었을 때 평균은 좋아졌지만 B0006의 수명 예측은 오히려 나빠졌다.
- 이 모델은 "같은 종류의 배터리라면 믿을 만한" 수준이며, 처음 보는 종류의 배터리에서는 정확도가 크게 떨어진다.
